# Convex Configuration
# Get these values by running: npx convex dev
# For production, use your Convex production deployment URL

# Deployment used by `npx convex dev` (local development)
CONVEX_DEPLOYMENT=dev:your-deployment-name

# Public Convex URL (required for client)
NEXT_PUBLIC_CONVEX_URL=https://your-deployment-name.convex.cloud

# App Closure Configuration (optional)
# Set to "true" or "1" to close the app to public access
# APP_CLOSED=false

# Optional bypass secret for developer access when app is closed
# Visit /bypass?token=YOUR_SECRET to gain access
# APP_BYPASS_SECRET=your-secret-token-here
